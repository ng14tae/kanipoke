<div class="min-h-screen py-6 px-6">
    <div class="container mx-auto max-w-4xl px-4 sm:px-8">
        <div class="text-center px-4 py-8 sm:py-12">
        <!-- キャッチコピー -->
        <p class="text-base sm:text-lg md:text-xl text-white mb-6 leading-relaxed">
            「インディアンポーカー」を<br>
            模したトランプゲームです。<br>
            アカウントを作成して始めよう！
        </p>

        <!-- ユーザー登録フォーム -->
        <div class="flex justify-center mt-8 sm:mt-12">
            <div class="w-full max-w-[280px] bg-white rounded-2xl shadow-2xl p-4 sm:p-6 backdrop-blur-lg">
            <h1 class="text-xl sm:text-2xl font-bold text-gray-900 mb-4 sm:mb-6 text-center">
                ユーザー登録
            </h1>

            <%= form_with model: @user, class: "flex flex-col gap-3 sm:gap-4", data: { turbo: false } do |f| %>
                <div>
                <%= f.label :first_name, "二つ名", class: form_label_classes %>
                <%= f.text_field :first_name,
                    placeholder: "例：ほげがめ",
                    class: form_input_classes %>
                </div>

                <p class="text-gray-600 text-center text-sm sm:text-base my-0">の</p>

                <div>
                <%= f.label :last_name, "ニックネーム", class: form_label_classes %>
                <%= f.text_field :last_name,
                    placeholder: "例：てー",
                    class: form_input_classes %>
                </div>

                <div>
                <%= f.label :password, "パスワード", class: form_label_classes %>
                <%= f.password_field :password, class: form_input_classes %>
                </div>

                <div>
                <%= f.label :password_confirmation, "パスワード（確認）", class: form_label_classes %>
                <%= f.password_field :password_confirmation, class: form_input_classes %>
                </div>

                <%= f.submit "登録", class: "#{button_classes(type: :primary)} w-full mt-2 sm:mt-3" %>
            <% end %>

            <div class="text-center mt-4 sm:mt-6">
                <%= link_to 'ログインページへ', login_path,
                    class: "inline-block text-blue-900 underline font-semibold text-xs sm:text-sm px-3 py-1.5 rounded-md transition-all duration-200 hover:text-blue-800 hover:bg-blue-50" %>
            </div>
            </div>
        </div>
        </div>
    </div>
</div>