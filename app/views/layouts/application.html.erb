<!DOCTYPE html>
<html>
  <head>
    <link rel="manifest" href="<%= pwa_manifest_path(format: :json) %>">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <title><%= page_title(yield(:title)) %></title>
    <!-- ファビコンの設定 -->
    <%= favicon_link_tag 'favicon.ico' %>

    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="mobile-web-app-capable" content="yes">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <meta name="rails-env" content="<%= Rails.env %>">

    <!-- 🎨 OGP設定を追加 -->
    <meta property="og:title" content="<%= content_for?(:og_title) ? yield(:og_title) : page_title(yield(:title)) %>" />
    <meta property="og:description" content="<%= content_for?(:og_description) ? yield(:og_description) : '手軽に楽しめる対戦ミニゲーム！' %>" />
    <meta property="og:image" content="<%= content_for?(:og_image) ? yield(:og_image) : image_url('og-default.png') %>" />
    <meta property="og:url" content="<%= request.original_url %>" />
    <meta property="og:type" content="website" />
    <meta property="og:site_name" content="KANIDIAN POKER" />

    <!-- Twitter Card設定 -->
    <meta name="twitter:card" content="summary_large_image" />
    <meta name="twitter:title" content="<%= content_for?(:og_title) ? yield(:og_title) : page_title(yield(:title)) %>" />
    <meta name="twitter:description" content="<%= content_for?(:og_description) ? yield(:og_description) : '手軽に楽しめる対戦ミニゲーム！' %>" />
    <meta name="twitter:image" content="<%= content_for?(:og_image) ? yield(:og_image) : image_url('og-default.png') %>" />

    <%= yield :head %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload", preload: false %>
    <%= javascript_include_tag "application", "data-turbo-track": "reload", defer: true %>

    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=DotGothic16&display=swap" rel="stylesheet">

    <!-- Google tag (gtag.js) -->
    <script async src="https://www.googletagmanager.com/gtag/js?id=G-4BHLGX03DH"></script>
    <script>
      window.dataLayer = window.dataLayer || [];
      function gtag(){dataLayer.push(arguments);}
      gtag('js', new Date());

      gtag('config', 'G-4BHLGX03DH');
    </script>

    <!-- ヘッダー固定したので -->
    <style>
      body {
        padding-top: 5rem; /* ヘッダーの高さに合わせて調整 */
      }
    </style>
    </head>

    <body class="[&_input]:text-black [&_textarea]:text-black [&_select]:text-black"
      style="background-color: #7CADED; font-family: DotGothic16, sans-serif; color: #FFF691;">

    <!-- ヘッダーナビゲーション -->
    <%= render 'shared/header' %>
    <%= render 'shared/flash' %>

    <!-- メインコンテンツエリア -->
    <main class="flex-grow">
      <%= yield %>
    </main>

    <footer>
      <div class="text-center mb-4">
        <%= render 'shared/footer' %>
      </div>
    </footer>

        <!-- FABメニュー -->
      <%= render "shared/fab_menu" %>
        <!-- FAB 用の JS は app/javascript/application.js に移動しました -->

  </body>
</html>
