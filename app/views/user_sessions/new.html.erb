<div class="min-h-screen py-2 px-6">

  <div class="container mx-auto" style="max-width: 1152px; padding: clamp(1rem, 3vw, 2rem);">
    <div class="text-center px-4" style="padding: clamp(2rem, 6vw, 3rem) 1rem;">
      <!-- キャッチコピー -->
      <p style="font-size: clamp(1rem, 3vw, 1.25rem); margin-bottom: 1.5rem; color: #ffffff; line-height: 1.6;">
        「インディアンポーカー」を<br>
        模したトランプゲームです。<br>
        アカウントを作成して始めよう！
      </p>

      <!-- スペーシング調整（レスポンシブ） -->
      <div style="margin: clamp(1.5rem, 5vw, 3rem) 0;"></div>

      <!-- ログインフォーム -->
      <div class="flex justify-center">
        <div style="width: 100%; max-width: 280px; background-color: #ffffff; border-radius: 1rem; box-shadow: 0 20px 25px -5px #00000033, 0 10px 10px -5px #0000001a; padding: clamp(1rem, 3vw, 1.5rem); backdrop-filter: blur(10px);">
          <h1 class="text-2xl font-bold text-gray-900 mb-6 text-center" style="font-size: clamp(1.25rem, 3.5vw, 1.75rem); margin-bottom: clamp(1rem, 3vw, 1.5rem);">ログイン</h1>

          <%= form_with url: login_path, style: "display: flex; flex-direction: column; gap: clamp(0.75rem, 2.5vw, 1rem);", data: { turbo: false } do |f| %>
            <div>
              <%= f.label :first_name, class: "block text-sm font-medium text-gray-700 mb-1", style: "font-size: clamp(0.75rem, 2vw, 0.875rem); margin-bottom: 0.25rem; color: #374151;" %>
              <%= f.text_field :first_name, placeholder: "例：ほげがめ", class: "w-full px-3 py-2 border border-gray-300 rounded-md shadow-sm focus:outline-none focus:ring-2 focus:ring-blue-500 focus:border-blue-500", style: "width: 80%; max-width: 200px; margin: 0 auto; display: block; padding: clamp(0.5rem, 1.5vw, 0.625rem) 0.75rem; border: 2px solid #d1d5db; border-radius: 0.375rem; font-size: clamp(0.75rem, 2vw, 0.875rem); transition: all 0.2s ease; color: #808080;" %>
            </div>

            <p class="text-gray-600 text-center" style="color: #6b7280; font-size: clamp(0.875rem, 2.5vw, 1rem); text-align: center; margin: 0;">の</p>

            <div>
              <%= f.label :last_name, class: "block text-sm font-medium text-gray-700 mb-1", style: "font-size: clamp(0.75rem, 2vw, 0.875rem); margin-bottom: 0.25rem; color: #374151;" %>
              <%= f.text_field :last_name, placeholder: "例：てー", class: "w-full px-3 py-2 border border-gray-300 rounded-md shadow-sm focus:outline-none focus:ring-2 focus:ring-blue-500 focus:border-blue-500", style: "width: 80%; max-width: 200px; margin: 0 auto; display: block; padding: clamp(0.5rem, 1.5vw, 0.625rem) 0.75rem; border: 2px solid #d1d5db; border-radius: 0.375rem; font-size: clamp(0.75rem, 2vw, 0.875rem); transition: all 0.2s ease; color: #808080;" %>
            </div>

            <div>
              <%= f.label :password, class: "block text-sm font-medium text-gray-700 mb-1", style: "font-size: clamp(0.75rem, 2vw, 0.875rem); margin-bottom: 0.25rem; color: #374151;" %>
              <%= f.password_field :password, class: "w-full px-3 py-2 border border-gray-300 rounded-md shadow-sm focus:outline-none focus:ring-2 focus:ring-blue-500 focus:border-blue-500", style: "width: 80%; max-width: 200px; margin: 0 auto; display: block; padding: clamp(0.5rem, 1.5vw, 0.625rem) 0.75rem; border: 2px solid #d1d5db; border-radius: 0.375rem; font-size: clamp(0.75rem, 2vw, 0.875rem); transition: all 0.2s ease; color: #808080;" %>
            </div>

            <%= f.submit "ログイン",
                style: "display: inline-block; width: 80%; background-color: #facc15; color: #1e3a8a; font-weight: 900; font-size: clamp(0.875rem, 2.5vw, 1rem); padding: clamp(0.625rem, 2vw, 0.75rem) 1rem; border-radius: 0.5rem; text-decoration: none; border: 2px solid #eab308; box-shadow: 0 10px 15px -3px #0000001a; transition: all 0.2s ease; transform: translateY(0); cursor: pointer; margin-top: clamp(0.5rem, 1.5vw, 0.75rem); color: #808080;",
                onmouseover: "this.style.backgroundColor='#f59e0b'; this.style.transform='translateY(-4px) scale(1.02)'; this.style.boxShadow='0 20px 25px -5px #00000033'; this.style.borderColor='#d97706';",
                onmouseout: "this.style.backgroundColor='#facc15'; this.style.transform='translateY(0) scale(1)'; this.style.boxShadow='0 10px 15px -3px #0000001a'; this.style.borderColor='#eab308';",
                onmousedown: "this.style.transform='translateY(-2px) scale(0.98)';",
                onmouseup: "this.style.transform='translateY(-4px) scale(1.02)';" %>
          <% end %>

          <div class="text-center" style="text-align: center; margin-top: clamp(1rem, 3vw, 1.5rem);">
            <%= link_to '登録ページへ', new_user_path,
                style: "display: inline-block; color: #1e3a8a; text-decoration: underline; font-weight: 600; font-size: clamp(0.75rem, 2vw, 0.875rem); padding: 0.375rem 0.75rem; border-radius: 0.375rem; transition: all 0.2s ease;",
                onmouseover: "this.style.color='#1e40af'; this.style.backgroundColor='#f0f9ff';",
                onmouseout: "this.style.color='#1e3a8a'; this.style.backgroundColor='transparent';" %>
          </div>
        </div>
      </div>
    </div>
  </div>

  <!-- モバイル用追加スタイル -->
  <style>
    @media (max-width: 768px) {
      .container {
        padding: 1rem !important;
      }

      /* タッチデバイス用のホバー効果調整 */
      @media (hover: none) {
        input[type="submit"]:active {
          transform: translateY(-2px) scale(0.98) !important;
        }

        a:active {
          transform: scale(0.95) !important;
        }
      }

      /* フォーム要素のタッチ対応 */
      input[type="text"], input[type="password"] {
        font-size: 16px !important; /* iOS Safariでズームを防ぐ */
      }
    }

    @media (max-width: 280px) {
      .min-h-screen {
        padding: 1rem 0.5rem !important;
      }

      /* 小さなスマートフォンでのフォーム調整 */
      form > div {
        margin-bottom: 0.75rem !important;
      }
    }

    /* フォーカス状態の改善 */
    input:focus {
      border-color: #3b82f6 !important;
      box-shadow: 0 0 0 3px #bfdbfe !important;
    }
  </style>
</div>